#include <stdio.h>
#define MAX 100

typedef struct Matrix{
    int definition[MAX][MAX];
    int linha[MAX];
    int coluna[MAX];
    int transposta[MAX];

} Matrix;

Matrix matriz_nova(int definition[][]){
    Matrix A;
    A.definition[MAX][MA]

}


Matrix matriz_soma(Matrix a, Matrix b)

Matrix matriz_subtracao(Matrix a,Matrix b)

Matrix matriz_multiplicacao(Matrix a,Matrix b)

Matrix matriz_transposta(Matrix a)
int main(){



    return 0;
}